# 绿立方 Phase 4 - 支付结算模块 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成支付结算体系：微信支付接入（JSAPI）、平台托管结算、佣金计算、内部账户、提现（供应商+司机）

**Architecture:** 后端 NestJS 模块化（payment/settlement），订单确认收货后触发自动结算 T+1，提现走「商户转账到零钱」通道。MVP 阶段支持模拟支付（方便测试），接入微信支付时替换实现。

**Tech Stack:** NestJS + Prisma + 微信支付 v3 API (wechatpay-node-v3 或 axios 直连)

---

## 前置条件
- Phase 3 已完成，订单模块可用
- Prisma Schema 已包含 Payment / Settlement / Withdraw / UserAccount 表
- （可选）微信商户号已申请，有 JSAPI 支付和商户转账权限

---

## Task 1: 后端支付模块 - 微信支付 + 模拟支付

**Files:**
- Create: `packages/server/src/payment/payment.module.ts`
- Create: `packages/server/src/payment/payment.service.ts`
- Create: `packages/server/src/payment/payment.controller.ts`
- Create: `packages/server/src/payment/dto/pay-order.dto.ts`
- Modify: `packages/server/src/app.module.ts` (import PaymentModule)
- Modify: `packages/server/src/config/configuration.ts` (添加 wechatPay 配置)

- [ ] **Step 1: 创建 DTO**

```typescript
// pay-order.dto.ts
import { IsInt, IsOptional, IsString } from 'class-validator';

export class PayOrderDto {
  @IsInt()
  orderId: number;

  @IsOptional()
  @IsString()
  openid?: string;
}
```

- [ ] **Step 2: 更新配置 configuration.ts**

在配置对象中添加：
```typescript
wechatPay: {
  appid: process.env.WECHAT_APPID || '',
  mchId: process.env.WECHAT_PAY_MCH_ID || '',
  apiKey: process.env.WECHAT_PAY_API_KEY || '',
  certPath: process.env.WECHAT_PAY_CERT_PATH || '',
  keyPath: process.env.WECHAT_PAY_KEY_PATH || '',
  notifyUrl: process.env.WECHAT_PAY_NOTIFY_URL || 'http://localhost:3000/api/payment/notify',
},
```

- [ ] **Step 3: 创建 PaymentService**

```typescript
import { Injectable, BadRequestException } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { PrismaService } from '../prisma/prisma.service';
import { PayOrderDto } from './dto/pay-order.dto';

@Injectable()
export class PaymentService {
  constructor(
    private prisma: PrismaService,
    private configService: ConfigService,
  ) {}

  private generateOutTradeNo() {
    return `WX${Date.now()}${Math.random().toString(36).slice(2, 10).toUpperCase()}`;
  }

  async pay(userId: number, dto: PayOrderDto) {
    const order = await this.prisma.order.findUnique({
      where: { id: dto.orderId },
      include: { orderItems: true },
    });
    if (!order) throw new BadRequestException('订单不存在');
    if (order.buyerId !== userId) throw new BadRequestException('无权支付');
    if (order.status !== 'pending_accept' && order.status !== 'preparing') {
      throw new BadRequestException('订单状态不正确');
    }

    const existing = await this.prisma.payment.findFirst({
      where: { orderId: order.id, status: 'success' },
    });
    if (existing) throw new BadRequestException('订单已支付');

    const outTradeNo = this.generateOutTradeNo();
    const payment = await this.prisma.payment.create({
      data: {
        orderId: order.id,
        userId,
        amount: order.actualAmount,
        payMethod: 'wechat',
        outTradeNo,
        status: 'pending',
      },
    });

    const mockMode = this.configService.get<string>('nodeEnv') !== 'production' ||
      !this.configService.get<string>('wechatPay.mchId');

    if (mockMode) {
      return this.mockPay(payment.id, order.id);
    }

    return {
      paymentId: payment.id,
      outTradeNo,
      amount: order.actualAmount,
      // 实际接入时这里返回微信支付参数
      payParams: { appId: '', timeStamp: '', nonceStr: '', package: '', signType: '', paySign: '' },
    };
  }

  async mockPay(paymentId: number, orderId: number) {
    await this.prisma.payment.update({
      where: { id: paymentId },
      data: { status: 'success', paidAt: new Date() },
    });
    return { paymentId, mock: true, message: '模拟支付成功' };
  }

  async handleNotify(body: any) {
    const { outTradeNo, transactionId } = body;
    const payment = await this.prisma.payment.findUnique({ where: { outTradeNo } });
    if (!payment || payment.status === 'success') return { code: 'SUCCESS' };
    await this.prisma.payment.update({
      where: { id: payment.id },
      data: { status: 'success', paidAt: new Date(), transactionId },
    });
    return { code: 'SUCCESS' };
  }
}
```

- [ ] **Step 4: 创建 PaymentController**

```typescript
import { Controller, Post, Body, UseGuards, Request, Get, Param } from '@nestjs/common';
import { PaymentService } from './payment.service';
import { PayOrderDto } from './dto/pay-order.dto';
import { JwtAuthGuard } from '../common/guards/jwt-auth.guard';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';

@ApiTags('支付')
@Controller('payment')
export class PaymentController {
  constructor(private paymentService: PaymentService) {}

  @Post('pay')
  @UseGuards(JwtAuthGuard)
  @ApiBearerAuth()
  @ApiOperation({ summary: '发起支付' })
  pay(@Request() req: any, @Body() dto: PayOrderDto) {
    return this.paymentService.pay(req.user.id, dto);
  }

  @Post('notify')
  @ApiOperation({ summary: '微信支付回调（内部用）' })
  notify(@Body() body: any) {
    return this.paymentService.handleNotify(body);
  }
}
```

- [ ] **Step 5: 创建 PaymentModule 并注册到 AppModule**

- [ ] **Step 6: 验证编译（pnpm build）**

- [ ] **Step 7: 提交代码**

---

## Task 2: 后端结算模块 - 自动结算 + 佣金 + 提现

**Files:**
- Create: `packages/server/src/settlement/settlement.module.ts`
- Create: `packages/server/src/settlement/settlement.service.ts`
- Create: `packages/server/src/settlement/settlement.controller.ts`
- Create: `packages/server/src/settlement/dto/withdraw.dto.ts`
- Modify: `packages/server/src/app.module.ts` (import SettlementModule)

- [ ] **Step 1: 创建 DTO**

```typescript
// withdraw.dto.ts
import { IsString, IsNumber, Min, IsOptional } from 'class-validator';

export class WithdrawDto {
  @IsNumber()
  @Min(1)
  amount: number;

  @IsOptional()
  @IsString()
  withdrawMethod?: string;
}
```

- [ ] **Step 2: 创建 SettlementService（核心）**

```typescript
import { Injectable, BadRequestException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { WithdrawDto } from './dto/withdraw.dto';

@Injectable()
export class SettlementService {
  constructor(private prisma: PrismaService) {}

  async settleOrder(orderId: number) {
    const order = await this.prisma.order.findUnique({
      where: { id: orderId },
      include: { orderItems: true, supplier: true },
    });
    if (!order) throw new BadRequestException('订单不存在');
    if (order.status !== 'completed') throw new BadRequestException('订单未完成');

    const existing = await this.prisma.settlement.findFirst({ where: { orderId } });
    if (existing) return existing;

    const commissionRate = 0.02;
    const commissionAmount = Math.round(order.totalAmount * commissionRate * 100) / 100;
    const supplierAmount = order.totalAmount - commissionAmount;
    const driverAmount = 0;

    const settlement = await this.prisma.settlement.create({
      data: {
        orderId,
        supplierId: order.supplierId,
        orderAmount: order.totalAmount,
        commissionRate,
        commissionAmount,
        supplierAmount,
        driverAmount,
        status: 'pending',
      },
    });

    await this.ensureUserAccount(order.supplierId);
    await this.prisma.userAccount.update({
      where: { userId: order.supplierId },
      data: { pendingBalance: { increment: supplierAmount } },
    });

    return settlement;
  }

  async ensureUserAccount(userId: number) {
    const existing = await this.prisma.userAccount.findUnique({ where: { userId } });
    if (existing) return existing;
    return this.prisma.userAccount.create({
      data: { userId, balance: 0, pendingBalance: 0, frozenAmount: 0 },
    });
  }

  async processScheduledSettlements() {
    const orders = await this.prisma.order.findMany({
      where: { status: 'completed', completedAt: { lte: new Date(Date.now() - 24 * 60 * 60 * 1000) } },
    });
    for (const order of orders) {
      const settlement = await this.settleOrder(order.id);
      await this.prisma.settlement.update({
        where: { id: settlement.id },
        data: { status: 'settled', settledAt: new Date() },
      });
      await this.prisma.userAccount.update({
        where: { userId: settlement.supplierId },
        data: {
          pendingBalance: { decrement: settlement.supplierAmount },
          balance: { increment: settlement.supplierAmount },
        },
      });
    }
    return { processed: orders.length };
  }

  async getAccount(userId: number) {
    const account = await this.ensureUserAccount(userId);
    return account;
  }

  async getSettlements(userId: number, role: string, params: any) {
    const { page = 1, pageSize = 20 } = params;
    const skip = (page - 1) * pageSize;
    const where: any = {};
    if (role === 'supplier') where.supplierId = userId;
    if (role === 'driver') where.driverId = userId;
    const [list, total] = await Promise.all([
      this.prisma.settlement.findMany({ where, skip, take: pageSize, orderBy: { createdAt: 'desc' } }),
      this.prisma.settlement.count({ where }),
    ]);
    return { list, total, page, pageSize };
  }

  async withdraw(userId: number, role: string, dto: WithdrawDto) {
    const account = await this.ensureUserAccount(userId);
    if (account.balance < dto.amount) throw new BadRequestException('余额不足');
    if (dto.amount < 50) throw new BadRequestException('最低提现金额 50 元');

    await this.prisma.userAccount.update({
      where: { userId },
      data: { balance: { decrement: dto.amount }, frozenAmount: { increment: dto.amount } },
    });

    return this.prisma.withdraw.create({
      data: {
        userId,
        role,
        amount: dto.amount,
        fee: 0,
        actualAmount: dto.amount,
        status: 'pending',
        withdrawMethod: dto.withdrawMethod || 'wechat',
      },
    });
  }

  async getWithdrawals(userId: number, params: any) {
    const { page = 1, pageSize = 20 } = params;
    const skip = (page - 1) * pageSize;
    const [list, total] = await Promise.all([
      this.prisma.withdraw.findMany({ where: { userId }, skip, take: pageSize, orderBy: { createdAt: 'desc' } }),
      this.prisma.withdraw.count({ where: { userId } }),
    ]);
    return { list, total, page, pageSize };
  }
}
```

- [ ] **Step 3: 创建 SettlementController**

```typescript
import { Controller, Get, Post, Body, Query, UseGuards, Request } from '@nestjs/common';
import { SettlementService } from './settlement.service';
import { WithdrawDto } from './dto/withdraw.dto';
import { JwtAuthGuard } from '../common/guards/jwt-auth.guard';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';

@ApiTags('结算')
@Controller('settlement')
@UseGuards(JwtAuthGuard)
@ApiBearerAuth()
export class SettlementController {
  constructor(private settlementService: SettlementService) {}

  @Get('account')
  @ApiOperation({ summary: '获取我的账户余额' })
  getAccount(@Request() req: any) {
    return this.settlementService.getAccount(req.user.id);
  }

  @Get('list')
  @ApiOperation({ summary: '结算记录' })
  getSettlements(@Request() req: any, @Query() query: any) {
    return this.settlementService.getSettlements(req.user.id, req.user.currentRole || 'supplier', query);
  }

  @Post('withdraw')
  @ApiOperation({ summary: '申请提现' })
  withdraw(@Request() req: any, @Body() dto: WithdrawDto) {
    return this.settlementService.withdraw(req.user.id, req.user.currentRole || 'supplier', dto);
  }

  @Get('withdrawals')
  @ApiOperation({ summary: '提现记录' })
  getWithdrawals(@Request() req: any, @Query() query: any) {
    return this.settlementService.getWithdrawals(req.user.id, query);
  }
}
```

- [ ] **Step 4: 创建 SettlementModule 并注册到 AppModule**

- [ ] **Step 5: 验证编译（pnpm build）**

- [ ] **Step 6: 提交代码**

---

## Task 3: 小程序端 - 支付 + 结算提现

**Files:**
- Create: `packages/miniapp/src/api/payment.ts`
- Create: `packages/miniapp/src/api/settlement.ts`
- Create: `packages/miniapp/src/pages/payment/index.vue`
- Create: `packages/miniapp/src/pages/settlement/index.vue`
- Create: `packages/miniapp/src/pages/settlement/withdraw.vue`
- Modify: `packages/miniapp/src/pages.json`
- Modify: `packages/miniapp/src/pages/order/confirm.vue` (结算按钮)

- [ ] **Step 1: 封装 API（payment.ts + settlement.ts）**

payment.ts：
```typescript
import { request } from './request';

export function payOrder(data: { orderId: number }) {
  return request({ url: '/payment/pay', method: 'POST', data });
}
```

settlement.ts：
```typescript
import { request } from './request';

export function getAccount() {
  return request({ url: '/settlement/account', method: 'GET' });
}

export function getSettlementList(params?: any) {
  return request({ url: '/settlement/list', method: 'GET', data: params });
}

export function applyWithdraw(data: { amount: number }) {
  return request({ url: '/settlement/withdraw', method: 'POST', data });
}

export function getWithdrawalList(params?: any) {
  return request({ url: '/settlement/withdrawals', method: 'GET', data: params });
}
```

- [ ] **Step 2: 支付页 pages/payment/index.vue**
  - 展示订单号、支付金额
  - 支付方式选择（微信支付）
  - 「立即支付」按钮（MVP 调用模拟支付）
  - 支付成功后跳转订单详情

- [ ] **Step 3: 结算中心页 pages/settlement/index.vue**
  - 顶部：账户余额、待结算金额
  - Tab：结算记录 / 提现记录
  - 「去提现」按钮
  - 绿色主题

- [ ] **Step 4: 提现页 pages/settlement/withdraw.vue**
  - 显示可提现余额
  - 提现金额输入框
  - 提现方式选择（微信）
  - 「确认提现」按钮
  - 最低 50 元提示

- [ ] **Step 5: 更新 pages.json（添加 3 个新页面）**

- [ ] **Step 6: 更新确认下单页**
  - 提交订单成功后跳转到支付页，带上 orderId

- [ ] **Step 7: 在个人中心添加「我的钱包」入口（供应商/司机角色可见）**

- [ ] **Step 8: 验证编译 + 提交代码**

---

## Task 4: 运营后台 - 财务/结算管理

**Files:**
- Create: `packages/admin/src/api/settlement.ts`
- Create: `packages/admin/src/views/finance/settlement.vue`
- Create: `packages/admin/src/views/finance/withdrawal.vue`
- Modify: `packages/admin/src/router/index.ts`
- Modify: `packages/admin/src/layouts/Default.vue`

- [ ] **Step 1: 封装 API（settlement.ts）**
- [ ] **Step 2: 结算记录页**：表格展示结算流水（订单、供应商、金额、佣金、结算状态、时间）、筛选、分页
- [ ] **Step 3: 提现审核页**：表格展示提现申请（用户、角色、金额、状态、时间）、操作（通过/拒绝）
- [ ] **Step 4: 更新路由和菜单（财务管理分组）**
- [ ] **Step 5: 验证编译 + 提交代码**

---

## Phase 4 完成检查清单

- [ ] 支付 API 可用（模拟支付模式，非生产环境自动成功）
- [ ] 账户余额系统可用（UserAccount: balance/pendingBalance/frozenAmount）
- [ ] 订单确认收货后自动结算（T+1 逻辑，佣金 2%）
- [ ] 提现功能可用（最低 50 元）
- [ ] 小程序支付页、结算中心、提现页可用
- [ ] 运营后台财务/结算管理可用
- [ ] 三端编译通过

---

*Phase 4 计划完。*
